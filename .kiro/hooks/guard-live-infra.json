{
  "version": "v1",
  "hooks": [
    {
      "name": "Guard Live Infrastructure",
      "trigger": "PreToolUse",
      "description": "Asks for confirmation before shell commands that deploy, publish, spend money on cloud resources, or rewrite git history. NADA ships with billable API keys, so an accidental public deploy has real cost.",
      "matcher": "execute_pwsh|control_pwsh_process",
      "action": {
        "type": "command",
        "command": "powershell -NoProfile -ExecutionPolicy Bypass -File .kiro/hooks/scripts/guard-live-infra.ps1",
        "timeout": 15
      }
    }
  ]
}